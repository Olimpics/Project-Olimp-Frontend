import { Navigation } from '@/components/header/Navigation'

export const Header: React.FC = () => {
    return (
        <header className="bg-main py-4 align-center">
            <Navigation />
        </header>
    )
}
